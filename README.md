# Ex01_softwaredesign

## Cài đặt và Chạy client
- Vào thư mục client cd client
1. **Cài đặt các phụ thuộc**:
   Trước khi chạy project, bạn cần cài đặt các phụ thuộc. Để cài đặt, mở terminal và chạy lệnh sau:

   ```bash
   npm install
   Sau khi cài đặt xong, bạn có thể chạy project bằng lệnh: npm run dev

